const router = require('express').Router();

router.get('/products', );
router.post('/products', );

//Metodos http por id

router.get('/products/:id', )
router.post('/products/:id', )
router.delete('/products/:id', )
router.parch('/products/:id', )

module.exports = router;